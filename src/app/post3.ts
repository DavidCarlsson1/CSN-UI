import { Component, Inject, Input } from "@angular/core";

export interface Post3 {
    headline: string;
    text: string;
    hyperlink: string;
    author: string;
    startDate: string;
    endDate: string;
}